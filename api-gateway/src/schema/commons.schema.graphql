type AuthPayload {
  user: User
  errors: [ErrorPayload]
}

type ErrorPayload {
	field: String
	message: [String]
}

type PageInfo {
  startCursor: String!
  endCursor: String!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}
